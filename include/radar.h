#ifndef __BIRCH_RADAR_CHART__
#define __BIRCH_RADAR_CHART__

#include "base.h"

namespace birch
{
    /*class RadarGraph : public DataGraph<RadarGraph>
	{

    protected:
		std::vector<float> data ;
		float radius, point_radius ;
		float guide_thickness, plot_thickness ;
		bool  dotted_plot, dotted_guide ;

	public:
		RadarGraph(const std::initializer_list<float>& d, float r) :
			data{d}, radius{r}, dotted_plot{false}, point_radius{4} {}

		void render();
		void makePlotLineDotted(bool v) { dotted_plot = v; }
		void makeGuideLineDotted(bool v) { dotted_guide = v; }

	};*/
}


#endif // __BIRCH_RADAR_CHART__
